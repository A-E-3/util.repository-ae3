# util.repository-ae3

Bootstraps an `ae3` workspace from scratch — clones/updates the set of repos an `ae3` workspace needs. Run the install one-liner in `sh-scripts/install-ae3-repository.sh`, or `sh-scripts/install-myx.ae3-devel-workspace.sh` for the full devel-workspace variant.
