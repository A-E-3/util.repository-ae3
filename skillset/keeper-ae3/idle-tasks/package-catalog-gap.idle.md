`SKILL.md`'s "What lives here" section admits it doesn't yet catalogue what each package-taxonomy branch (`sys.pkg.s4.lcl.*`, `sys.pkg.l2.tgt.*`, `pkg.lib.*`, etc.) actually does — it says to learn a package's purpose by reading it, not by pattern-matching the name. When a daily pass (this task or `file-comment-gap.md`) leaves you with a genuine, confirmed understanding of one package's real purpose — not just its name — add a short, grounded note for it in that section. Example of exactly this: the `JdbcLocalS4` dead-scaffolding finding (an apparent generic JDBC backend that's actually unfinished stub code, never wired in) — worth a line in "What lives here" so a future reader doesn't re-discover it from scratch.

Don't guess from the naming pattern alone — only add what you've actually confirmed by reading the source. Log the addition like any other idle-day entry, as a new dated file in `processed/`.
